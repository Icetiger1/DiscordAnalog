<div class="alert @CssClass">
    @Message
</div>

@code {
    [Parameter] public string Message { get; set; } = string.Empty;
    [Parameter] public bool IsError { get; set; }

    private string CssClass => IsError ? "alert-error" : "alert-success";
}